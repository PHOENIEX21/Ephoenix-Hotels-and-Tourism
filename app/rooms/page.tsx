import Image from 'next/image';
import { hotels, rooms, naira } from '../../lib/data';
import PublicRoomExtras from '../../components/PublicRoomExtras';
export default function RoomsPage(){return <main><div className="page-intro"><div className="eyebrow">Room directory</div><h1>Find your room.</h1><p>A single view of all EPhoenix room types across Main, Annex I, and Annex II. Rates are shown per night; branch charges are listed in each property&apos;s details.</p></div><section className="section" style={{paddingTop:20}}><div className="filter-row">{hotels.map(h=><span className="filter" key={h.slug}>{h.shortName} · {h.rooms} rooms</span>)}<span className="filter">24-hour stay model</span></div><div className="grid-4">{rooms.map(r=><article className="card" key={r.hotel+r.name}><div className="room-photo">{r.image && <Image src={r.image} fill sizes="(max-width: 760px) 100vw, 25vw" alt={r.name} />}</div><div className="room-body"><div className="label">{hotels.find(h=>h.slug===r.hotel)?.shortName}</div><h3>{r.name}</h3><div className="price">{naira(r.price)} <small>/ night</small></div><p className="muted">{r.roomNumbers.length} room{r.roomNumbers.length===1?'':'s'} available in this type · Deposit {naira(r.deposit)}</p><PublicRoomExtras hotelSlug={r.hotel} roomTypeSlug={r.slug} /></div></article>)}</div></section></main>}

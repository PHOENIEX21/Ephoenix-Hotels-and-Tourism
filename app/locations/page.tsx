import { hotels, policiesByHotel } from '../../lib/data';
export default function LocationsPage(){return <main><div className="page-intro"><div className="eyebrow">Visit EPhoenix</div><h1>Three addresses.<br/>One warm welcome.</h1><p>All three properties sit in and around GRA, Ilorin, making it easy to choose the setting and room that suit your stay.</p></div><section className="section" style={{paddingTop:20}}><div className="locations">{hotels.map(h=><article className="location-copy" key={h.slug}><div className="eyebrow">{h.shortName}</div><h2>{h.name.replace('E-Phoenix Hotels and Tourism ','').replace('E-Phoenix Hotel ','')}</h2><p>{h.address}</p><p><strong>{h.phone}</strong></p><p>Charges are {h.vatMode}; {h.serviceNote}.</p></article>)}</div></section><section className="section"><div className="section-head"><div className="eyebrow">Good to know</div><h2>Stay policy</h2></div><div className="locations">{hotels.map(h=><article className="location-copy" key={h.slug+'-policy'}><h3>{h.shortName}</h3>{policiesByHotel[h.slug].map(policy=><div className="policy" key={policy.item}>• {policy.item}: {policy.detail}</div>)}</article>)}</div></section></main>}
